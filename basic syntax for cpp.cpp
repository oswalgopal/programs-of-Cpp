#include<iostream>    //This is used to add header files which contain the declaration for the identifier cout and cin  


using namespace std; //this defines a scope for the identifiers that are used in a program (std is a namespace where the standard class libraries are defined)


int main()
{

cout<<"This program is to understand basic syntax of c++" ;             //cout is a identifier or pre-defined object that represents the standard output in c++ 
                                                                        // << is called a insertion or put to operator which send the contents of the variable on its right to the object on its left
                                                                        

return 0;
}
